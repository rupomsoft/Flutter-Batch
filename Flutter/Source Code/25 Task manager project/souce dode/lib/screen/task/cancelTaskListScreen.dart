import 'package:flutter/material.dart';

class cancelTaskListScreen extends StatelessWidget {
  const cancelTaskListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
